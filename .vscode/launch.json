{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "name": "Launch Data Node",
            "request": "launch",
            "preLaunchTask": "build",
            "env": {
                "RUST_LOG": "trace"
            },
            "program": "${workspaceFolder}/target/debug/data-node",
            "args": [
                "-c",
                "${workspaceFolder}/etc/data-node.yaml",
                "-l",
                "${workspaceFolder}/etc/data-node-log.yaml"
            ]
        }
    ]
}