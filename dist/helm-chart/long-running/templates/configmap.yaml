apiVersion: v1
kind: ConfigMap
metadata:
  name: client-promtail-sidecar-conf
  labels:
    app: client-promtail-sidecar
data:
  promtail.yaml: |
    scrape_configs:
      - job_name: system
        static_configs:
        - targets:
          - localhost
          labels:
            job: ${NAME}-replication-log
            __path__: /logs/replication.log
        - targets:
          - localhost
          labels:
            job: ${NAME}-client-log
            __path__: /logs/client.log
